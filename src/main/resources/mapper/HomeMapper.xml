<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.oneshot.common.HomeMapper">

    <select id="login" parameterType="HomeVO" resultType="int">
        select count(*) from id_pw
                        where pw = #{password}
                        and employee_no = #{employeeNo}
    </select>

</mapper>